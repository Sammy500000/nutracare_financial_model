[project]
name = "vndp-demand"
version = "0.0.0"
description = "VNDP service (bounded context): demand"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/demand"]
