[project]
name = "vndp-simulation"
version = "0.0.0"
description = "VNDP service (bounded context): simulation"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/simulation"]
