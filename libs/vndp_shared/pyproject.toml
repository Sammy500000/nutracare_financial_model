[project]
name = "vndp-shared"
version = "0.0.0"
description = "VNDP shared lib: vndp_shared"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vndp_shared"]
