[project]
name = "vndp-api-gateway"
version = "0.0.0"
description = "VNDP API gateway / BFF (FastAPI)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api_gateway"]
